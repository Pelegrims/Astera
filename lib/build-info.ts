/**
 * Bumped manually with every zip Claude hands over. Rendered as a tiny
 * grey line at the bottom of /bazi so "which version is my browser
 * actually showing?" is answerable in two seconds — this team deploys by
 * uploading files through the GitHub web UI, where partial uploads have
 * silently shipped stale pages more than once.
 */
export const BUILD_TAG = "2026-08-05 f · period picker (year + bazi month)";
